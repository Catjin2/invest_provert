# 🎓 대학원 및 기술 면접 대비 프로젝트 심층 분석 가이드 (Deep Dive & Study)

본 문서는 프로젝트의 설계 사상, 적용 공식, 자료구조 및 알고리즘의 컴퓨터 공학적/금융공학적 가치에 대한 예상 면접 질문과 학술적 모범 답안을 포함하고 있습니다. 질문 하단의 **`📝 개인 메모 및 답변 보완`**란에 자신만의 대답을 적거나 수정하며 학습할 수 있습니다.

---

## 1. Concurrency & Python Internals (동시성 및 파이썬 내부 동작)

### Q1. 파이썬의 GIL(Global Interpreter Lock) 제약에도 불구하고 `ThreadPoolExecutor`를 데이터 수집 모듈에 채택한 구체적인 엔지니어링적 이유는 무엇입니까?
* **이론적 배경 및 메커니즘**:
  * **GIL (Global Interpreter Lock)**: CPython 인터프리터는 한 프로세스 안에서 동시에 두 개 이상의 스레드가 파이썬 바이트코드(Bytecode)를 실행하지 못하도록 락을 겁니다. 이는 파이썬 내부 객체의 가비지 컬렉션(Reference Counting) 메모리 관리 구조를 단순화하고 스레드 안전성(Thread-safety)을 얻기 위한 설계 결정입니다.
  * **I/O-Bound vs CPU-Bound**: GIL은 CPU 연산 작업(예: 행렬 곱셈, 복잡한 반복문)에서는 병렬 처리를 완전히 무력화하지만, **I/O 작업(네트워크 패킷 송수신, 파일 읽기/쓰기, DB 쿼리 대기)**에서는 다릅니다. 파이썬 표준 라이브러리의 소켓 C-extension 모듈은 시스템 콜(System Call)을 날려 대기 상태(Blocking)에 들어갈 때, **자발적으로 GIL을 일시 해제(Release)**합니다. 덕분에 대기 상태 동안 다른 스레드가 CPU 연산 권한을 얻어 실행될 수 있습니다.
* **프로젝트 실제 코드 연계**:
  * [invest_data_provider.py](file:///c:/Users/catji/Projects/invest_provert/invest_data_provider.py)에서 `yf.download`나 `requests.get`을 실행할 때, 스레드는 대기 상태로 빠지며 GIL을 반환합니다. 이때 다른 스레드가 동시 다발적으로 웹 요청을 보내므로 병렬 다운로드가 수행됩니다.
* **모범 답안**:
  > "CPython의 GIL 때문에 파이썬 멀티스레드는 CPU 연산의 병렬 처리에는 부적합하지만, 주가 데이터 수집은 대기 시간이 99%인 대표적인 **I/O-Bound 작업**입니다. `requests.get`이나 `yfinance` 다운로드와 같은 소켓 통신 모듈은 시스템 콜을 발생시킬 때 내부적으로 GIL을 해제하므로, `ThreadPoolExecutor`를 통해 최대 15개 종목의 데이터 수집을 병렬화하여 병목 대기 시간을 1/N로 줄였습니다."
  CPython 인터프리터는 한 프로세그 내에서 동시에 두 개 이상의 스레드가 파이썬 바이트 코드를 실행하지 못하도록 제한하는 GIL 을 가지고 있음. 이는 파이썬 내부의 레퍼런스 카우닝 메모리 관리 구조를 단순화하고 스레드 안정성을 얻기 위함임. 하지만 GIL 은 CPU연산 작업에서만 병렬처리를 하는 무력화할 뿐, I/O bound 작업에서는 다르게 작동함.파이썬 표준 라이브러리 소켓 통신 모듈은 시스템 콜을  날려 대기 상태에 들어갈 떄 자발적으로 GIL 을 일시적으로 해제함. 현재 프로젝트의 데이터 수집은 대기 시간이 99%인 대표적인 I.O bound 작업이므로, 하나의 스레드가 패킷을 대기하는 동안 다른 스레드가 CPU 자원을 얻어 동시에 병렬로 다음 네트워크 요청을 보낼 수 있음. 이 덕분에 15개 자산군 수집속도를 1?N 수준으로 단축할 수 있음.

📝 **개인 메모 및 답변 보완**:
* 

---

### Q2. OS 스레드 기반의 `ThreadPool` 대신, 경량 비동기 루프인 `asyncio` (코루틴)을 사용하여 프로젝트를 작성하지 않은 이유는 무엇인가요?
* **이론적 배경 및 메커니즘**:
  * **스레드(Thread) 오버헤드**: OS 스레드는 커널 수준에서 관리하는 자원(Kernel-level Resource)으로, 생성 시점마다 고유의 프로세서 레지스터 세트, Program Counter 및 기본 8MB 수준의 가상 메모리 스택 영역을 할당받습니다. 스레드 간 전환 시 커널 모드로의 진입 및 CPU 레지스터 값을 복원하는 **컨텍스트 스위칭 비용**이 수반됩니다.
  * **코루틴(Coroutine)과 이벤트 루프(Event Loop)**: `asyncio`는 단일 OS 스레드 내에서 작동하며, 유저 공간(User Space)에서 상태 장치(State Machine) 형태로 실행을 일시 중단(`await`)하고 복귀합니다. 메모리 오버헤드가 몇 KB 수준으로 매우 낮고 커널 모드 전환 비용이 없습니다.
  * **동기-비동기 라이브러리 간섭 문제**: 파이썬의 표준 `sqlite3` 라이브러리와 `yfinance` 모듈은 내부 소켓/파일 디스크립터가 **동기식 블로킹(Blocking)**으로 설계되어 있습니다. 이들을 `asyncio` 이벤트 루프 위에서 직접 실행하면, 동기식 호출이 이벤트 루프가 돌아가는 유일한 스레드를 점유하여 루프 전체가 얼어버리는(Stall) 중대한 아키텍처적 문제를 초래합니다.
* **모범 답안**:
  > "만약 프로젝트의 모든 외부 드라이버(HTTP, Database)를 비동기 전용 드라이버(`aiohttp`, `aiosqlite` 등)로 교체했다면 `asyncio`가 메모리 및 컨텍스트 스위칭 성능 면에서 우수했을 것입니다. 
  > 하지만 금융권 및 업계에서 널리 검증된 `yfinance`, `sqlite3` 라이브러리는 모두 내부가 동기식 블로킹으로 작동합니다. 이를 `asyncio` 루프에서 그대로 가동하면 싱글 스레드 이벤트 루프 전체가 멈추는 병목이 생깁니다. 따라서 동기 라이브러리의 안정성과 인터페이스를 그대로 활용하면서 수집 병렬성을 확보하기 위해 `ThreadPoolExecutor`를 활용하여 각각의 I/O를 개별 스레드로 격리하여 실행하는 설계를 선택했습니다."

📝 **개인 메모 및 답변 보완**:
* 

---

## 2. Databases & Performance (데이터베이스 설계 및 동시성)

### Q1. 다중 스레드(ThreadPoolExecutor) 환경에서 SQLite 데이터베이스의 `database is locked` 에러를 예방하기 위해 커넥션을 어떻게 제어했습니까?
* **이론적 배경 및 메커니즘**:
  * **SQLite 락킹 상태 모델**: SQLite는 파일 기반 RDBMS로 데이터의 무결성을 지키기 위해 5단계 락 상태(`UNLOCKED` ➔ `SHARED` ➔ `RESERVED` ➔ `PENDING` ➔ `EXCLUSIVE`)를 거칩니다. 다중 스레드에서 단일 커넥션 세션 객체를 공유하거나 트랜잭션을 수 분 동안 열어두면, 하나의 스레드가 `EXCLUSIVE` 락을 잡고 쓸 때 다른 스레드는 대기하다가 타임아웃되어 `sqlite3.OperationalError: database is locked` 예외를 던집니다.
  * **Context Manager & Short-lived Connection**: 이를 해결하기 위한 전략은 **커넥션 유지 시간을 극단적으로 좁히는 것**입니다. 데이터베이스 접근이 필요한 찰나의 순간에만 커넥션을 열어 쿼리를 날리고 커밋(Commit) 후 즉시 닫아야(`close`) 합니다.
* **프로젝트 실제 코드 연계**:
  * [invest_db.py](file:///c:/Users/catji/Projects/invest_provert/invest_db.py)의 `execute` 및 `save_prices` 메서드 구현:
    ```python
    def execute(self, query, params=()):
        conn = sqlite3.connect(self.db_path) # 호출 시점 커넥션 생성
        cursor = conn.cursor()
        cursor.execute(query, params)
        conn.commit()
        conn.close() # 작업 즉시 자원 반환 및 락 해제
    ```
* **모범 답안**:
  > "SQLite는 파일 기반 공유 잠금 모델을 사용하므로, 스레드들이 하나의 글로벌 데이터베이스 커넥션을 공유해 쓰면 트랜잭션 잠금 경합이 일어나 `database is locked` 에러가 발생하기 쉽습니다. 
  > 저는 이를 방지하기 위해 데이터베이스 접근 헬퍼 클래스인 `AntiGravityDB` 내부에서 커넥션을 영구 유지하지 않고, 매 쿼리나 데이터 수이브 작업(`save_prices` 등)이 호출될 때마다 함수 내부에서 세션을 새로 맺고 작업이 끝나는 즉시 닫아주는 **단기 트랜잭션 세션 격리 아키텍처**를 적용했습니다. 이를 통해 파일 락 보유 시간을 최소화하여 스레드 간 락 경합을 원천 차단했습니다."

📝 **개인 메모 및 답변 보완**:
* 

---

### Q2. 데이터의 정합성과 수집 파이프라인의 멱등성(Idempotency)을 보장하기 위해 테이블 스키마에 적용된 장치는 무엇인가요?
* **이론적 배경 및 메커니즘**:
  * **멱등성(Idempotency)**: 수학적 정의로 $f(f(x)) = f(x)$ 이며, 시스템에 동일 연산을 여러 번 요청하거나 실패에 따른 재시도를 거듭하더라도 최종 상태가 단 한 번 정상 수행되었을 때와 완벽히 일치함을 뜻합니다.
  * **중복 키 위반(Primary Key Violation)**: 단순 `INSERT` 쿼리는 재실행 시 무결성 오류를 내며 시스템 크래시를 유발합니다. 
* **프로젝트 실제 코드 연계**:
  * `price_cache` 테이블 스키마 정의 (라인 137~148):
    ```sql
    CREATE TABLE IF NOT EXISTS price_cache (
        ticker TEXT,
        date TEXT,
        open REAL,
        high REAL,
        low REAL,
        close REAL,
        adj_close REAL,
        volume INTEGER,
        PRIMARY KEY (ticker, date) -- 복합 기본키 선언
    )
    ```
  * `save_prices` 내 Upsert 쿼리 (라인 277~280):
    ```sql
    INSERT OR REPLACE INTO price_cache (ticker, date, open, high, low, close, adj_close, volume)
    VALUES (?, ?, ?, ?, ?, ?, ?, ?)
    ```
* **모범 답안**:
  > "수집 파이프라인의 불안정성(API 타임아웃 등)에 대응하기 위해 수집 모듈의 멱등성을 확보했습니다. 데이터베이스 레벨에서 `ticker`와 `date` 컬럼을 묶어 **복합 기본키(Composite Primary Key)**를 설정하여 데이터 중복 문제를 방지했습니다. 
  > 그리고 데이터 적재 시 `INSERT OR REPLACE` 구문을 활용해, 네트워크 순서 꼬임이나 스레드 재실행으로 인해 동일 날짜 데이터가 중복 입력되어도 충돌 없이 기존 로컬 캐시를 최신 값으로 정화(Upsert)하여 갱신하도록 처리해 데이터 정합성을 철저히 보증했습니다."

📝 **개인 메모 및 답변 보완**:
* 

---

## 3. Architecture & Software Engineering (설계 사상 및 SOLID)

### Q1. 가격 데이터를 수집할 때 `yfinance` 모듈을 직접 호출하지 않고 `DataProvider`로 추상화하여 얻은 유지보수 및 테스트적 강점은 무엇인가요?
* **이론적 배경 및 메커니즘**:
  * **의존성 역전 원칙 (DIP, Dependency Inversion Principle)**: 고수준 모듈(예: 트레이딩 엔진, 스크리닝 모듈)이 저수준 모듈(예: 특정 라이브러리인 yfinance)의 구체적인 내부 구조에 직접 의존하면 안 되며, 양쪽 모두 **추상화된 인터페이스**에 의존해야 유지관리가 수월해집니다.
  * **Facade 패턴 및 어댑터 패턴**: `DataProvider`는 주식 가격 취득이라는 복잡한 이면의 논리(yfinance 크래시, Direct API 폴백, 로컬 캐시 동기화)를 내부로 감추고 클라이언트에게는 오직 `get_history()`라는 하나의 일관된 진입점을 제공합니다.
* **프로젝트 실제 코드 연계**:
  * `invest_strategy.py`에서는 외부 API 모듈이 아닌 오직 `DataProvider` 인스턴스를 통해서만 데이터를 가져옵니다.
* **모범 답안**:
  > "기존의 모든 분석 및 리포팅 로직이 외부 라이브러리인 `yfinance`에 강하게 결합(Tight Coupling)되어 있던 형태를 개선하기 위해, 데이터를 관리하고 취득하는 통로인 `DataProvider` 객체를 캡슐화했습니다. 
  > 덕분에 외부 네트워크의 세부 동작은 데이터 제공자 내부의 책임으로 가두어졌고, 메인 도메인 영역은 단순화되었습니다. 이 추상화 구조는 테스트 시 완벽한 이점을 제공하는데, 인터넷 환경에 의존하지 않고 단위 테스트를 하고자 할 때 가짜 주가 리스트를 반환하는 **Mock Object를 DataProvider 자리에 주입(DI)**하여 비즈니스 논리의 정상 작동 여부를 빠르게 테스트할 수 있습니다."

📝 **개인 메모 및 답변 보완**:
* 

---

### Q2. Notion 리포트와 메인 트레이딩 실행 주기를 결합하지 않고 데이터베이스 큐 기반의 비동기 워커로 구성한 이유는 무엇인가요?
* **이론적 배경 및 메커니즘**:
  * **관심사의 분리 (Separation of Concerns)**: 금융 자산 운용 엔진(트레이더, 오더 핸들러)은 신속하고 중단 없는 동작이 생명입니다. 반면 Notion 보고서 전송, 그래프 이미지 생성 및 렌더링, GitHub 이미지 업로드 등은 외부 네트워크 지연과 오버헤드가 매우 큽니다.
  * **동기식 연동의 위험**: 이 둘을 동기식 단일 프로세스로 묶으면 Notion API 서버가 응답 불능(Outage)에 빠졌을 때, 트레이딩 엔진 전체가 행(Hang)에 걸려 포트폴리오를 미처 정리하지 못하고 엄청난 매매 손실(Systemic Crash)로 이어질 수 있습니다.
* **프로젝트 실제 코드 연계**:
  * `invest_main.py`에서는 Notion 연동 메서드를 직접 타지 않고, 분석 로그 페이로드를 DB 테이블 `reporting_queue`에 밀어 넣은 뒤, 백그라운드 스레드로 `invest_reporter_worker.py`를 작동시키는 구조입니다.
* **모범 답안**:
  > "보고서 전송과 같은 I/O 중심의 서브시스템이 다운되거나 네트워크 지연을 겪을 때, 메인 주문 집행 엔진 전체가 멈춰 서는 참사를 막기 위해 **관심사 분리 및 장애 격리(Fault Isolation) 설계**를 적용했습니다. 
  > 메인 시뮬레이션 프로세스는 계산 결과를 데이터베이스의 `reporting_queue` 테이블에 적재하는 것으로 자신의 역할을 빠르게 끝내고 종료되며, 실제 보고서 가공 및 Notion 전송은 비동기로 가동되는 독립 스레드 워커가 도맡아 수행하므로 핵심 운용 로직의 안정성과 무정지 구동을 담보할 수 있게 되었습니다."

📝 **개인 메모 및 답변 보완**:
* 

---

## 4. Algorithmic Precision & Finance (수학적/금융공학적 공식 검증)

### Q1. 가격 모멘텀 스코어 산출 시 단순히 수익률($R_{12}$) 외에 결정계수($R^2$)를 결합하여 스코어링하는 통계학적, 투자학적 당위성은 무엇인가요?
* **이론적 배경 및 수학적 증명**:
  * **복리 성장과 로그 선형화**: 주가 $P_t$의 복리 성장은 $P_t = P_0 (1+r)^t$로 지수형식으로 표현됩니다. 여기에 자연로그를 취하면 $\ln(P_t) = \ln(P_0) + t \cdot \ln(1+r)$ 이 되어, 주식 가격은 **로그 차트상에서 완벽한 1차 직선(Linear)**을 이루게 됩니다.
  * **최소자승법(OLS)과 결정계수($R^2$)**: 회귀선이 실제 주가 데이터의 변동성을 얼마나 잘 설명하는지 나타내는 지표가 결정계수 $R^2$ 입니다.
    $$R^2 = 1 - \frac{\sum (y_i - \hat{y}_i)^2}{\sum (y_i - \bar{y})^2} = 1 - \frac{SS_{res}}{SS_{tot}}$$
  * **추세 품질(Trend Quality)**: 수익률($R_{12}$)만 높고 주가가 널뛰기하는 종목은 $R^2$가 극히 낮습니다. 반면 매일 일정한 속도로 흔들림 없이 우상향하는 탄탄한 자산은 $R^2$가 `1.0`에 수렴합니다.
* **모범 답안**:
  > "단순 과거 수익률만으로 ETF를 스크리닝하면 최근 갑작스러운 이상 폭등으로 수익률만 착시 현상처럼 높아진 불량 변동성 자산이 진입하여 밸류에이션 반전(Mean Reversion) 손실을 입기 쉽습니다. 
  > 저는 이를 교정하기 위해 주가 데이터를 로그 스케일로 선형화한 뒤 최소자승법(OLS) 회귀 분석을 통해 **결정계수 $R^2$**를 얻었습니다. 이 $R^2$는 가격이 흔들림 없이 매일 일정한 속도로 견고하게 우상향하고 있는지를 보여주는 '추세 품질' 척도입니다. 이를 수익률과 곱한 $R_{12} \times R^2$ 점수를 사용하여 가격 변동 노이즈가 낮고 상승 관성의 강도가 탄탄한 초우량 자산만을 선별해 낼 수 있었습니다."

📝 **개인 메모 및 답변 보완**:
* 

---

### Q2. 데이터 감사 모듈에서 이상치 감지 임계치로 자산별 $3\sigma$ 변동성과 15% 정적 한계를 결합한 하이브리드 공식을 왜 적용했습니까?
* **이론적 배경 및 수학적 증명**:
  * **일일 변동성 스케일링**: 연간 변동성 $\sigma_{ann}$을 일일 변동성 $\sigma_{daily}$으로 환산하려면, 브라운 운동의 확률 과정 법칙에 따라 **시간의 제곱근 규칙(Square Root of Time Rule)**을 적용해 $\sqrt{252}$로 나누어주어야 합니다.
    $$\sigma_{daily} = \frac{\sigma_{ann}}{\sqrt{252}}$$
  * **3시그마 규칙 ($3\sigma$)**: 가우시안 확률 분포 하에서 평균값으로부터 표준편차의 3배 범위($3\sigma$)를 초과하여 벗어나는 사건이 일어날 통계적 확률은 단 `0.27%`에 불과합니다. 즉, 이 범위를 이탈하면 자연스러운 시장 가격 등락이 아니라 데이터 피드 왜곡이나 소스 데이터 훼손으로 판정하는 과학적 근거가 됩니다.
  * **하한선 `0.15` (15%) 결합 배경**: 자산 배분 대상인 단기 국채(SHY)나 초안전자산은 평상시 변동성($\sigma$)이 거의 `0`에 가깝습니다. 만약 $3\sigma$만 고집하면 0.5% 수준의 자연스러운 일일 변동마저 에러로 오탐하는 기저 효과(Base Effect) 오류가 생깁니다. 이를 막고자 최하 15%의 완충 하한선을 `max` 함수로 결합한 것입니다.
* **모범 답안**:
  > "자산군에 따라 평소 움직이는 변동폭의 크기가 전혀 다릅니다. 단기 국채의 미세한 왜곡 에러도 잡아내고 원유와 같은 고변동성 자산의 정상적인 폭등락은 에러로 오인하지 않기 위해 자산의 최근 60일 변동성을 기반으로 한 **$3\sigma$ (발생 확률 0.27% 미만의 통계적 임계점) 채널**을 구축했습니다. 
  > 단, 변동성이 너무 좁아진 평화로운 시기에 기저 효과로 인해 정상 가격 변동을 에러로 잡는 오류를 예방하기 위해 최소 하한 문턱값(15%)을 `max(3 * daily_vol, 0.15)` 형태로 결합하여 데이터 품질 감사의 실효성과 정밀도를 극대화했습니다."

📝 **개인 메모 및 답변 보완**:
* 

---

### Q3. "만약 2020년 3월 팬데믹이나 2008년 금융위기처럼 시장 전체가 동시 다발적으로 무너지는 블랙스완이 발생하면, 실제 하락 가격임에도 감사 모듈이 $3\sigma$ 초과로 인해 모두 데이터 오류(FAIL)로 판정해 버리는 한계가 있습니다. 이를 시스템적으로 어떻게 보완하겠습니까?"
* **이론적 배경 및 수학적 증명**:
  * **체계적 위험(Systemic Risk) vs 비체계적 위험(Unsystematic Risk)**: 단일 기업이나 단일 API의 피드 장애는 비체계적 위험이며 개별 독립적으로 일어납니다. 반면, 글로벌 매크로 위기는 체계적 위험으로 포트폴리오 내의 자산들이 동시에 강한 상관관계(Correlation)를 가지며 가격이 폭락하는 동조화 현상(Co-movement)을 보입니다.
* **모범 답안**:
  > "개별 자산 관점의 이상치 판정이 지닌 한계점을 보완하기 위해 **다변수 동시성 교차 검증(Multivariate Cross-Validation) 알고리즘**을 도입할 수 있습니다. 
  > 단일 종목의 $3\sigma$ 이탈은 단순 데이터 소스 오류일 확률이 매우 높지만, 포트폴리오 유니버스 전체(예: 15개 자산군 중 7개 이상)가 동일 날짜에 동시에 임계치를 이탈하는 것은 데이터 오류가 아닌 **시장 전체의 체계적 위험(Systemic Crisis)이 실현된 상황**으로 분류해야 합니다. 
  > 따라서 자산 이탈 비율이 일정 상한선 이상이거나, 시장 대표 지수인 QQQ/SPY의 급락이 교차 관측되면 감사 모듈은 데이터 FAIL 판정을 우회 유예하고, 이를 실제 매크로 패닉 상황으로 자동 분류하여 안전하게 전량 현금화(Global Exit) 절차를 수행하도록 상태 머신을 고도화할 계획입니다."

📝 **개인 메모 및 답변 보완**:
* 
